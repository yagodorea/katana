[package]
name = "katana-viewer"
version = "0.1.0"
edition = "2021"

[dependencies]
katana-core = { path = "../katana-core" }
eframe = { version = "0.31", features = ["glow"] }
glow = "0.16"
clap = { version = "4", features = ["derive"] }
