[package]
name = "katana-core"
version = "0.1.0"
edition = "2021"

[features]
default = ["parallel"]
parallel = ["rayon"]

[dependencies]
nalgebra = "0.33"
byteorder = "1"
thiserror = "2"
i_overlay = "4.4"
rayon = { version = "1", optional = true }

[dev-dependencies]
nalgebra = "0.33"
